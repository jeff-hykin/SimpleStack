FIXME,
    DONE: the watcher doesn't work on edits for global files 

make sure the back button/ history works properly 
start work on security and users 

convert newlines to break statements and other things 

figure out more security tools
    (Auth instead of jwt tokens )

make boilerplate a hidden folder

import management
    local style sheets
    add relative paths to loadPage() and add(component: to:)



error checking 
    check for a homepage
    extract the header + body in a better way

add Python functions as javascript functions on compile time 

add npm package support
    add local npm package support

add favicon.ico

in boilerplate
    get SimpleStackTools.js included instead of having to manually including it in the SimpleStackBase.html